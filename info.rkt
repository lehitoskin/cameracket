#lang setup/infotab

(define name "cameracket")
;(define scribblings '(("doc/libtoxcore-racket.scrbl" ())))

(define blurb '("Racket wrapper interface to webcam libraries."))
(define primary-file "main.rkt")
(define homepage "https://github.com/lehitoskin/cameracket/")

(define version "0.1")
(define release-notes '("Initial release."))

(define required-core-version "6.0.1")

(define deps '("base"))
